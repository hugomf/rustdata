# Fix QueryMethods Derive Macro — Complete Rewrite

## Current State

Both `query_methods_derive.rs` and `querymethod-example.rs` are broken:
- Macro generates trait + empty impl blocks (no method bodies)
- Column name extraction uses fragile string surgery on token stream Display output
- Only 3 operators generated (_, gt, like) — example uses 7
- Duplicate method names in single trait (CrudRepo vs QueryRepo same name)
- Circular `D: #trait_ident` bound on impl

## Goal

Replace `query_methods_derive.rs` with a complete rewrite that:
1. Generates **inherent `async fn` methods** (not a trait) on both `CrudRepository<BA, E>` and `QueryRepository<BA, E>`
2. Uses `darling::FromField` for attribute parsing (same pattern as `entity.rs`)
3. Generates all 7 comparison operators: `""`, `_ne`, `_gt`, `_gte`, `_lt`, `_lte`, `_like`
4. Generates compound `_and_` / `_or_` methods
5. Removes the circular bound issue entirely (no trait = no bounds)

## Files to Modify

1. `/Users/hugo/Projects/ff-rustdata/crates/rustdata-macros/src/query_methods_derive.rs` — **full replacement** with inherent method approach
2. `/Users/hugo/Projects/ff-rustdata/examples/querymethod-example.rs` — remove `use UserQueryMethods;`, update doc comment (trait no longer needed)

## Implementation Steps

### 1. Rewrite query_methods_derive.rs

- Add `use darling::FromField;` import
- Define `QMFieldAttrs` struct with `#[derive(Debug, Default, FromField)]` to parse `#[entity(id)]`, `#[entity(skip)]`, `#[entity(column = "...")]`
- Build `ColEntry { rust: String, col: String }` using `QMFieldAttrs::from_field(f)`
- Generate `crud_methods` and `qrepo_methods` token vectors
- For single-field ops (Eq, Ne, Gt, Gte, Lt, Lte):
  - Generate `pub async fn find_by_<field><suffix>(&self, value: V)` → `find_all_pred(&predicate).await`
  - Generate `pub async fn find_one_by_<field><suffix>(&self, value: V)` → `find_one_pred(&predicate).await`
  - For LIKE: `Into<String>` bound, `Predicate::Like { column, pattern }`
  - For QueryRepository: same methods but with leading `table: &str` arg
- For compound AND/OR: generate `find_by_<a>_and_<b>(va, vb)` / `_or_` using `Predicate::And/Or(vec![Eq { }, Eq { }])`
- Embed `find_all_pred` / `find_one_pred` directly in generated code (inherent methods)
- Emit two separate `impl<BA> CrudRepository<BA, E>` and `impl<BA> QueryRepository<BA, E>` blocks with correct where clauses

### 2. Fix querymethod-example.rs

- Remove `use UserQueryMethods;` line (replaced with comment)
- Update doc comment on line 16 from `generates the UserQueryMethods trait` to `generates inherent find_by_* on CrudRepository and QueryRepository`

### 3. Verify

Run `cargo run --example querymethod-example` and confirm output shows correct query results.
